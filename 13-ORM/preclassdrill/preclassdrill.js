

const preClassDrill = (input) => {

    // for (i=0; i < input.length; i++) {
    //     if (input[i] !== '') newstring += input[i];
    // }

    return input.toLowerCase(). replace(/\s/g, '');
};

module.exports = preClassDrill;

//Write a test for this.
